import React, { useContext } from "react";
import { RecipeProvider } from "../../utils/contexts/recipeContext";
const Recipes = () => 
  <RecipeProvider>
    
  </RecipeProvider>

export default Recipes;
